<!DOCTYPE html>
<html>
<head>
  <title>Ecom</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css", "application", media: "all" %>
  <%= javascript_importmap_tags %>

  <style>
    /* Custom Styles for Flash Messages */
    .notice {
      font-size: 24px; /* Adjust the size to your preference */
      font-weight: bold;
      color: #4CAF50; /* You can choose a different color */
      background-color: #f9f9f9;
      padding: 15px;
      margin: 10px 0;
      border-radius: 5px;
      text-align: center;
      border: 1px solid #4CAF50;
    }

    .alert {
      font-size: 20px; /* Adjust the size of the alert */
      font-weight: bold;
      color: #f44336; /* Red color for alerts */
      background-color: #f9f9f9;
      padding: 15px;
      margin: 10px 0;
      border-radius: 5px;
      text-align: center;
      border: 1px solid #f44336;
    }
  </style>

</head>

<body>
  <!-- Flash Messages -->
  <% if flash[:notice] %>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <% if flash[:alert] %>
    <p class="alert"><%= flash[:alert] %></p>
  <% end %>

  <!-- Main Content -->
  <%= yield %>

  <!-- Include Webpacker JavaScript Pack -->
</body>
</html>